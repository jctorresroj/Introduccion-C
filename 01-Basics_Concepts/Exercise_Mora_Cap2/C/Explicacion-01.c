#include <stdio.h>
// se utiliza la biblioteca Standard Input Output

int main(){
  // Siempre tiene que estar la funcion main como la funcion principal
  // Dentro de los parentesis despues de main no hay nada, luego no tiene parametros o argumentos
  // La palabra INT indica que debe devolver un valor entero

  printf("Este es mi primer programa . \n");
  // printf es una fucion propia de c(incluida en el encabezado)y sirve para mostar algun resultado.
  // el \n sirve para crrear una nueva linea dentro del programa
  return 0;
  // Retorna el valor de 0
}
